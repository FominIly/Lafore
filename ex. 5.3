// zerosamller.cpp : Этот файл содержит функцию "main". Здесь начинается и заканчивается выполнение программы.
//

#include "pch.h"
#include <iostream>

using namespace std;

int zerosmaller(int n, int p);

int main()
{
	int a;
	int b;
	cout << "Enter the numbers: ";
	cin >> a >> b;

	cout << zerosmaller(a, b);

	return 0;
}

int zerosmaller(int a, int p)
{
	if (a < p)
	{
		a = 0;
		return a;
	}
	
	if (p < a)
	{
		p = 0;
		return p;
	}
}
